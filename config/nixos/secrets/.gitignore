# Prevent committing unencrypted secrets and private keys
# Only encrypted .yaml files should be committed

# Ignore all files by default
*

# Allow encrypted secrets files
!secrets.yaml
!*-secrets.yaml

# Allow this .gitignore file
!.gitignore

# Ignore age private keys (should never be in git)
*.key
*.txt
key.txt

# Ignore any decrypted/temporary files
*.dec
*.tmp
*.bak
